[project]
name = "team-git-visualizer-backend"
version = "0.1.0"
description = "FastAPI backend for Team Git Visualizer"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pydantic-settings>=2.13.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.9",
]
